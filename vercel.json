{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm build:shared && pnpm --filter @voice-industrial-design/server build && pnpm --filter @voice-industrial-design/web build",
  "outputDirectory": "apps/web/.next"
}
